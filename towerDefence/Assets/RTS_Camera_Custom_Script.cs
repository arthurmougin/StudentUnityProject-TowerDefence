using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RTS_Camera_Custom_Script : MonoBehaviour
{
    // Start is called before the first frame update

    public float startingHeight = 2500f;
    void Start()
    {
        //transform.Translate(0,startingHeight,0);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
